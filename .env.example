# Zenti — environment variables (copy to .env, fill with the REAL Zenti Supabase project)
# NOTE: the session's Supabase MCP is currently pointed at the wrong project.
# Set these once the actual Zenti DB is connected.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
