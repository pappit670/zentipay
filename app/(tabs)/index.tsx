import { Screen } from '../../components/Screen';

export default function HomeTab() {
  return <Screen title="Home" subtitle="Balance, recent activity, savings & pools peek — v34 home (Law #2: untouched)." />;
}
