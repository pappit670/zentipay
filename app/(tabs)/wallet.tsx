import { Screen } from '../../components/Screen';

export default function WalletTab() {
  return <Screen title="Wallet" subtitle="Apple-Wallet card deck (Inspo 1) + card detail, perks, credit." />;
}
